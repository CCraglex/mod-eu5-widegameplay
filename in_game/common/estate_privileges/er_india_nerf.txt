er_indian_decentralization = {
    estate = nobles_estate

    potential = {
        has_estate_privilege = estate_privilege:er_indian_decentralization
    }

    # allow determines if it can be selected (empty/always true if they start with it)
    allow = {
    }

    # Modifiers must be placed in country_modifier per the documentation
    country_modifier = {
        global_nobles_estate_power = 0.5
        subject_loyalty = 15
		diplomatic_capacity = 1

		global_distance_from_capital_speed_propagation = -0.6
		global_levy_size_modifier = -0.5
    }

    # Evaluates whether you are legally allowed to click the revoke button
    can_revoke = {
        # Check if the country asset matches or exceeds the threshold
        "estate_power(estate_type:crown_estate)" >= 0.45
    }
}